;[ATT_ENDPOINTS]
;dsn=asterisk-rt
; Query MySQL compatível - busca endpoints 400-499
;readsql=SELECT GROUP_CONCAT(id SEPARATOR '|') FROM ps_endpoints WHERE id LIKE '4__' AND transport IS NOT NULL

[ATT_ENDPOINTS]
dsn=asterisk-mssql
readsql=SELECT STRING_AGG(CAST(id AS VARCHAR(50)), '|') FROM dbo.ps_endpoints WHERE id LIKE '4__' AND transport IS NOT NULL
